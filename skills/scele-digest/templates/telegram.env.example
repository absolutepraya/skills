# Telegram user-session credentials for scele-digest's bin/tg-window helper.
#
# Copied to ~/.config/scele-digest/telegram.env by `bin/init`.
#
# How to fill these in:
#
# 1. TELEGRAM_API_ID + TELEGRAM_API_HASH
#    Sign in at https://my.telegram.org/apps → fill the form → copy the
#    api_id and api_hash that appear.
#
# 2. TELEGRAM_SESSION_STRING
#    Generate once using the venv that `bin/init` created. The venv already
#    has telethon installed, so just run:
#
#      ~/.claude/skills/scele-digest/.venv/bin/python -c '
#      from telethon.sync import TelegramClient
#      from telethon.sessions import StringSession
#      api_id = int(input("api_id: "))
#      api_hash = input("api_hash: ")
#      with TelegramClient(StringSession(), api_id, api_hash) as c:
#          print(StringSession.save(c.session))
#      '
#
#    (Adjust the path if your agent installs skills somewhere other than
#    ~/.claude/skills/.) Follow the login prompts — phone number, then the
#    code Telegram texts you, then your 2FA password if you have one. Paste
#    the printed string into TELEGRAM_SESSION_STRING below. Keep it secret:
#    anyone with this string can act as you on Telegram.

TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_SESSION_STRING=
